
    {{>header}}
    <h1>
        HOME PAGE of News
    </h1>
    <div class="container ">
        <div class="card" style="">

            {{!-- {{#each articles}}
                <img src="{{this.urlToImage}}" class="card-img-top" alt="...">
                <div class="card-body">
                    <h3>{{this.title}}</h3>
                    <p class="card-text">{{this.description}}</p>
                </div>
            </div>
            {{/each}} --}}

            {{#each dataFinal}}
            <img src="{{urlToImage}}" class="card-img-top" id="image" alt="..." style="width:270px">
            <div class="card-body">
                <h4>{{title}}</h>
                <p class="card-text">{{description}}</p>
            </div>
            {{/each}}
        </div>
    </div>
    {{>footer}}

